<%= semantic_form_for @content,:html => {:multipart => true} do |content| %>

  <%= content.inputs %>
  <%= content.input :image, :as => :file %>
  <div class="td"><%#= image_tag(content.image_file_name.to_s, :size => "200x150")%> </div>
  <%= content.buttons %>

<% end %>
