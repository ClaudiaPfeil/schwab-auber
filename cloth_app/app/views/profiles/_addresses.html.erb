<%
# Titel:  Anschriften Verwaltung,dh. Hinzufügen von neuen Anschriften und
#         Anzeige aller Anschriften des Nutzers
# Autor:  Claudia Pfeil
# Datum:  23.11.10
%>

<h3><%= I18n.t(:profiles_addresses)%> </h3>
<p align="right">
  <% if can? :create, Address %>
    <%= link_to I18n.t(:new_address), :controller => :addresses, :action => :new, :user_id => @profile.id %>
  <%end%>
</p>

<% if @profile.addresses.count.to_i > 0  %>
  <%= render "all_addresses", :@profile => @profile %>
<% else %>
<p><%=h I18n.t(:add_address) %></p>
  <%= render "form_address", :profile => @profile %>
<% end %>