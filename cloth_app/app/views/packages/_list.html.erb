<%#
# Title:         Listenansicht aller Kleiderpakete
# Beschreibung:  Die Kleiderpakete bestellen dürfen nur Basis und Premium - Kunden.
                 Die Kleiderpakete mit Marken dürfen nur die Premium - Kunden bestellen.
                 Die neuen Kleiderpakete, dh. die in den letzten 24 Stunden erstellt wurden, dürfen nur von den Premium- Mitgliedern eingesehen und bestellt werden.
# Autor:         Claudia Pfeil
# Datum:         22.12.2010
%>

  <div id="filter" class=" span-4">
  <%= semantic_form_for Package.new, :url => search_remote_packages_path do |package| %>
    <%= package.input :sex, :as => :check_boxes, :collection => [[ link_to( I18n.t(:boy) + " (#{get_amount("sex", 0)})", search_remote_packages_path("sex=0"), :class => 'remote', :id => 'test'), "sex=0"],[ link_to( I18n.t(:girl) + " (#{get_amount("sex", 1)})", search_remote_packages_path("sex=1"), :class => 'remote'), "sex=1"]], :input_html => {:class => "required"}  %>
    <%= package.input :size, :as => :check_boxes, :collection  =>  make_link_strict(Package.new.get_contents("Size"), "size", false), :input_html => {:class => "required"} %><br>
    <%= package.input :saison, :as => :check_boxes, :collection => make_link(Package.new.get_contents("Saison"), "saison", true) %><br>
    <%= package.input :kind, :as => :check_boxes, :collection => make_link_special(Package.new.get_contents("Kind"), "kind") %><br>
  <% end %>
</div>


<div id="result">
  <div class="td first">&nbsp;</div>
  <div class="td span-4"><b><%= I18n.t(:serial_number)%></b> </div>
  <div class="td span-3"><b><%= I18n.t(:sex)%></b> </div>
  <div class="td span-2"><b><%= I18n.t(:size)%></b> </div>
  <div class="td span-2"><b><%= I18n.t(:amount_clothes)%></b> </div>
  <div class="td span-5"><b><%= I18n.t(:description)%></b> </div>
  <div class="td last">&nbsp;</div>



  <%= render :partial => "packages", :locales => @packages %>

</div>
